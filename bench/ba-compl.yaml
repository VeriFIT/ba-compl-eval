seminat:
  cmd: bin/seminator-wrap.sh $1 --postprocess-comp=0

spot:
  cmd: bin/spot-wrap.sh $1

safra:
  cmd: bin/goal-wrap.sh $1 -m safra

goal-df:
  cmd: bin/goal-wrap.sh $1

piterman:
  cmd: bin/goal-wrap.sh $1 -m piterman -r

schewe:
  cmd: bin/goal-wrap.sh $1 -m rank -r -tr -ro

# frib:
  # cmd: bin/goal-wrap.sh $1 -m fribourg

roll:
  cmd: bin/roll-wrap.sh $1

ranker:
  cmd: bin/ranker $1
